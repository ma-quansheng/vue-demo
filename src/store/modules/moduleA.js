import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)


const state = {
	count: 0,
	msg: ''
}

const actions={
	
}

const mutations={}

const getters={}

export default new Vuex.Store({
	state,
	mutations,
	actions,
	getters,
})
